/** @type {import('postcss-load-config').Config} */
const config = {
  images: {
    domains: ['firebasestorage.googleapis.com'],
  },
  plugins: {
    tailwindcss: {},
  },
};

export default config;
